from distutils.core import setup

setup(
        name='lbpLibrary',
        version='${PACKAGE_VERSION}',
        package_dir={ '': '${PROJECT_BINARY_DIR}'+"/LbpLibrary/python/" },
        packages=['lbp'],
        py_modules=['lbp2d', 'lbp3d','extend']
    )
